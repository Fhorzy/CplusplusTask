#include <iostream>
using namespace std;
int main() {
    float r, h;
    cout << "Input the radius of the cylinder: ";
    cin >> r;
    cout << "Input the height of the cylinder: ";
    cin >> h;
    cout << "The volume of a cylinder is: " << 3.14*r*r*h;
    return 0;
}